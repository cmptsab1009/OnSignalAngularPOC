import { CreateNotificationTokenResponse } from './create-notification-token-response';

describe('CreateNotificationTokenResponse', () => {
  it('should create an instance', () => {
    expect(new CreateNotificationTokenResponse()).toBeTruthy();
  });
});
